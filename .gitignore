# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.python-version

# Pipeline artifacts
probabilistic_forecasting/cache/
coastsat_segmentation/cache/
coastsat_segmentation/imagery/
coastsat_segmentation/models/
*.tif
*.tiff

# Cloned third-party code (CoastSat + symlinks)
external/
classification

# Deploy artifacts (Netlify builds publish/ from netlify.toml)
deploy/
publish/

# CoastSat ML output — NOT deployed yet (broken-polygon zones still pending).
# When ready to ship, force-add with: git add -f zones_ml.json
zones_ml.json

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Secrets
.env
*.pem
gee-service-account*.json
